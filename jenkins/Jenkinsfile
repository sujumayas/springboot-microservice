pipeline {
    agent any
    stages {
        
        stage('Test') {
            steps {
                sh './jenkins/scripts/test.sh'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        stage('Build') {
            steps {
                sh './jenkins/scripts/build.sh'
            }
        }
        stage('Build docker'){
            steps{
                sh './jenkins/scripts/build_docker.sh "./Dockerfile"'
            }
        }
        // stage('Deliver') {
        //     steps {
        //         sh './jenkins/scripts/deliver.sh'
        //     }
        // }
    }
}
